# forge-std (vendored)

foundry-rs/forge-std v1.15.0, vendored (src + licenses only) so a fresh
clone can `forge test` with no submodule step or network fetch. To bump:
replace src/ with the new tag's src/ and update this note + package.json.
Dual-licensed MIT / Apache-2.0 (see LICENSE-*).
